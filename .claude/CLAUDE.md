# Claude Project Rules

Follow the workflow defined in `AGENTS.md`.

Before finishing any coding task, always:

1. Run a separate agent-based code review when tool support exists.
2. Run tests.
3. Run lint.
4. Include concise results in the final summary.

If any step fails, fix and rerun. If blocked, report the blocker explicitly.
